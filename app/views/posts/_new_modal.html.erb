<%= form_with(model: @post) do |form| %>
  <div>
    <%= form.text_field :title, placeholder: "タイトル", maxlength: "50" %>
  </div>

  <div>
    <%= form.text_area :body, placeholder: "内容", maxlength: "1000" %>
  </div>
  <div>
    <%= form.submit value: "投稿" %>
  </div>
<% end %>